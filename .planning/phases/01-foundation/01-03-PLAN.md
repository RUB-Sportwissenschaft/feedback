---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified: []
autonomous: false
requirements:
  - FORM-04
  - DES-01

must_haves:
  truths:
    - "The form is visually correct and fully usable on a 375px mobile screen"
    - "RubFlama fonts render correctly"
    - "All 14 evaluation categories are visible with correct labels when navigating through all steps"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Phase 1 skeleton on a mobile device or mobile emulator. Confirm the RUB design system renders correctly, all 14 questions are present with correct labels, and the wizard navigation works through all 7 steps.

Purpose: Catch visual/layout issues before Phase 2 adds interactivity on top of this skeleton.
Output: Confirmation that the foundation is correct, or a list of issues to fix.
</objective>

<execution_context>
@C:/Users/kilia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kilia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of complete Phase 1 skeleton on mobile</name>
  <files>index.html</files>
  <action>
Present the completed index.html to the user for visual verification on a mobile device or Chrome DevTools mobile emulator.

What was built:
- Blue RUB header with "Lehrgangskritik Valmorel"
- Sticky progress bar (orange fill, step counter, section name)
- Step 0: Group selector (10 options) + optional name field
- Steps 1-5: All 14 rating questions with 5-tile widgets and endpoint labels
- Step 6: Ausbilder placeholder + allgemeines Freitext textarea + Absenden button
- Wizard navigation (Weiter/Zurueck) through all 7 steps

How to verify:
1. Open index.html in Chrome DevTools mobile view (375px width) or on a real phone
2. Verify: Blue header shows "Lehrgangskritik Valmorel" in RubFlama font (sans-serif, not serif)
3. Verify: NO orange accent bar below header
4. Verify: Progress bar is sticky (scroll within a long step -- bar stays visible)
5. Step 0: Tap group tiles -- one highlights orange. Verify all 10 groups visible (1-8 + SB1 + SB2). Name field present.
6. Tap "Weiter" -- advances to S1. Check progress shows "2 / 7"
7. Navigate through ALL 7 steps, checking:
   - Each section has its correct title
   - Each question has correct label text (compare with REQUIREMENTS.md)
   - Each question has 5 tiles labeled 1-5
   - Endpoint labels are correct (e.g., S1-02 shows "zu teuer" left, "angemessen" right)
   - S4-02 shows "3 = genau richtig" hint
8. Step 6: Ausbilder placeholder text visible, textarea present, "Absenden" button visible
9. Verify: No horizontal scrolling on any step
10. Verify: All tap targets comfortably tappable (no tiny buttons)

Resume signal: Type "approved" if everything looks correct, or describe any issues to fix.
  </action>
  <verify>User confirms visual correctness</verify>
  <done>User approves that the static skeleton matches the expected RUB design, all 14 questions are correctly labeled, the wizard navigation works, and the form is usable on a 375px mobile screen</done>
</task>

</tasks>

<verification>
Human confirms visual correctness on mobile viewport.
</verification>

<success_criteria>
User approves that the static skeleton matches the expected design, all 14 questions are correctly labeled, and the form is usable on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
